transcript on
if {[file exists gate_work]} {
	vdel -lib gate_work -all
}
vlib gate_work
vmap work gate_work

vcom -93 -work work {Sorting_Sys.vho}

vcom -93 -work work {C:/Users/Tomer Rotman/Desktop/New folder/embedded-systems-course/Sorting_Sys/sorting_control_TB.vhd}

vsim -t 1ps -L altera -L altera_lnsim -L cyclonev -L gate_work -L work -voptargs="+acc"  sorting_control_TB

add wave *
view structure
view signals
run -all
